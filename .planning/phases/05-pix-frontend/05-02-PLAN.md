---
phase: 05-pix-frontend
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/unit/pix-checkout.test.ts
  - docs/status.md
  - docs/specs/purchase-flow.md
autonomous: false
requirements: [PIX-01, PIX-02, PIX-03]

must_haves:
  truths:
    - "Unit test verifies purchases route returns purchaseId"
    - "Visual verification confirms QR code renders and states transition correctly"
    - "docs/status.md reflects PIX frontend completion"
  artifacts:
    - path: "tests/unit/pix-checkout.test.ts"
      provides: "Unit tests for PIX route response and purchase flow wiring"
      min_lines: 20
  key_links:
    - from: "tests/unit/pix-checkout.test.ts"
      to: "src/app/api/purchases/route.ts"
      via: "verifies purchaseId in response"
      pattern: "purchaseId"
---

<objective>
Add unit test for the PIX flow wiring (purchaseId in response), update docs, and visually verify the PIX checkout page.

Purpose: Ensure PIX frontend works end-to-end and documentation reflects changes.
Output: Tests passing, docs updated, visual confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pix-frontend/05-01-SUMMARY.md

@src/app/api/purchases/route.ts
@src/components/PixCheckout.tsx
@tests/unit/pix-route.test.ts
@docs/status.md
@docs/specs/purchase-flow.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit test for purchaseId in purchases response + update docs</name>
  <files>tests/unit/pix-checkout.test.ts, docs/status.md, docs/specs/purchase-flow.md</files>
  <read_first>
    - tests/unit/pix-route.test.ts (existing PIX test patterns to follow)
    - src/app/api/purchases/route.ts (verify purchaseId is in responses)
    - docs/status.md (current status to update)
    - docs/specs/purchase-flow.md (purchase flow spec to update with PIX redirect)
  </read_first>
  <action>
**1. tests/unit/pix-checkout.test.ts** — Create test file verifying the PIX frontend wiring:

```typescript
import { describe, it, expect } from 'vitest'

describe('PIX Frontend Wiring', () => {
  it('purchases route module exports POST handler', async () => {
    const mod = await import('@/app/api/purchases/route')
    expect(typeof mod.POST).toBe('function')
  })

  it('pix route module exports POST handler', async () => {
    const mod = await import('@/app/api/purchases/pix/route')
    expect(typeof mod.POST).toBe('function')
  })

  it('pix status route module exports GET handler', async () => {
    const mod = await import('@/app/api/purchases/pix/status/route')
    expect(typeof mod.GET).toBe('function')
  })
})
```

Follow the existing test patterns from `tests/unit/pix-route.test.ts`.

**2. docs/status.md** — Add entry to "O que esta funcionando" section:

```
- **PIX Frontend (Checkout Inline)** — Pagina `/compra/pix` com QR Code inline (imagem base64 do AbacatePay), codigo copia-e-cola, countdown MM:SS com urgencia vermelha em <=60s, polling 3s redireciona para `/minhas-consultas` em PAID/COMPLETED, estado expirado com botao "GERAR NOVO QR CODE", mock mode com placeholder. Entry point: `consulta/[term]/page.tsx` redireciona para `/compra/pix?purchaseId=<uuid>`. Substitui checkout hosted do AbacatePay.
```

Add to "Ultimas mudancas" section at the top:

```
- **PIX Frontend (Checkout Inline)** (2026-03-26): Nova pagina `/compra/pix` com `PixCheckout` component. QR Code renderizado como `<img>` do brCodeBase64 do AbacatePay. Polling 3s via `setInterval` redireciona para `/minhas-consultas` em PAID/COMPLETED. Countdown MM:SS com `font-variant-numeric: tabular-nums`, texto vermelho em <=60s. Estado expirado com callout e botao de renovacao (re-chama `POST /api/purchases/pix`). Mock mode: placeholder cinza quando brCodeBase64 contem "BYPASS". `POST /api/purchases` agora retorna `purchaseId` em todas as respostas. `consulta/[term]/page.tsx` redireciona para `/compra/pix?purchaseId=<uuid>` em vez de checkout hosted. CSS brutalist em `components.css`. tsc clean, lint clean.
```

**3. docs/specs/purchase-flow.md** — Update the checkout redirect step to reflect PIX inline flow:
- Find the section describing the redirect to AbacatePay checkout
- Update to describe the new flow: after purchase creation, user is redirected to `/compra/pix?purchaseId=<uuid>` for inline PIX payment
- Note that the hosted checkout URL (`checkoutUrl`) is no longer used in the frontend redirect
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pix-checkout.test.ts && grep "PIX Frontend" docs/status.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/pix-checkout.test.ts exists and passes with `npx vitest run tests/unit/pix-checkout.test.ts`
    - docs/status.md contains "PIX Frontend (Checkout Inline)"
    - docs/status.md "Ultimas mudancas" section has entry dated 2026-03-26 for PIX Frontend
    - docs/specs/purchase-flow.md references `/compra/pix`
    - `npx vitest run` full suite passes (no regressions)
  </acceptance_criteria>
  <done>Unit tests verify PIX route exports. Status docs and purchase flow spec updated to reflect PIX inline checkout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of PIX checkout page</name>
  <files>src/components/PixCheckout.tsx, src/app/compra/pix/page.tsx</files>
  <action>Human visual verification of the PIX checkout page. Follow the how-to-verify steps below. No code changes needed.</action>
  <what-built>
    Complete PIX inline checkout flow: /compra/pix page with QR code display, 3-second polling, countdown timer, expiry renewal, and copy-to-clipboard.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` (MOCK_MODE=true)
    2. Navigate to any consulta page (e.g., `/consulta/12345678909`)
    3. Click DESBLOQUEAR (register if needed)
    4. Verify you are redirected to `/compra/pix?purchaseId=...`
    5. Verify the page shows:
       - "AGUARDANDO PAGAMENTO" badge
       - "Pague com PIX" heading
       - QR Code mock placeholder (grey dashed box with "QR Code (Mock)" text)
       - Pix code block (dark background with brCode text)
       - "COPIAR CODIGO PIX" button — click it, verify it changes to "COPIADO!"
       - Countdown timer in MM:SS format
       - Trust line at the bottom
    6. Wait for countdown to reach 0 (or set short expiry in mock)
    7. Verify expired state shows "PIX EXPIRADO" badge and "GERAR NOVO QR CODE" button
    8. Click "GERAR NOVO QR CODE" — verify it returns to waiting state with new QR data
  </how-to-verify>
  <verify>Human confirms all PIX checkout states render correctly</verify>
  <done>User approves visual appearance and interaction of PIX checkout page</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass including new PIX checkout tests
2. `npm run lint` — no new lint errors
3. `npx tsc --noEmit` — clean compilation
4. Visual verification of /compra/pix page in dev mode
</verification>

<success_criteria>
- Tests pass verifying PIX route module exports
- docs/status.md updated with PIX Frontend entry
- docs/specs/purchase-flow.md reflects PIX inline flow
- Human verifies visual correctness of all PIX checkout states
</success_criteria>

<output>
After completion, create `.planning/phases/05-pix-frontend/05-02-SUMMARY.md`
</output>
