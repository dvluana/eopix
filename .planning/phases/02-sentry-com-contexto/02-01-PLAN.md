---
phase: 02-sentry-com-contexto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/inngest/process-search.ts
  - src/app/api/webhooks/abacatepay/route.ts
  - src/app/api/purchases/route.ts
  - src/app/api/admin/purchases/[id]/process/route.ts
  - tests/unit/sentry-context.test.ts
autonomous: true
requirements: [OBS-02, OBS-04]
user_setup:
  - service: sentry
    why: "Error monitoring with structured context"
    env_vars:
      - name: NEXT_PUBLIC_SENTRY_DSN
        source: "sentry.io -> Project Settings -> Client Keys (DSN)"
    dashboard_config:
      - task: "Create Sentry project (Next.js type)"
        location: "sentry.io -> Projects -> Create Project"

must_haves:
  truths:
    - "Pipeline errors in Sentry include purchase_code, pipeline_step, document_type, userId"
    - "Webhook errors in Sentry include purchase_code when available"
    - "Systemic errors (INSUFFICIENT_API_BALANCE, inngest_unreachable) tagged as error_category:infra"
    - "No CPF/CNPJ data leaks into Sentry (LGPD)"
    - "Sentry calls are no-op when DSN is absent (no runtime errors)"
  artifacts:
    - path: "tests/unit/sentry-context.test.ts"
      provides: "Unit tests verifying Sentry context structure at all 5 instrumentation points"
      min_lines: 80
    - path: "src/lib/inngest/process-search.ts"
      provides: "Sentry.withScope in catch block with pipeline/infra bifurcation"
      contains: "Sentry.withScope"
    - path: "src/app/api/webhooks/abacatepay/route.ts"
      provides: "Sentry.withScope in POST catch block"
      contains: "Sentry.withScope"
    - path: "src/app/api/purchases/route.ts"
      provides: "Sentry.withScope in outer catch block"
      contains: "Sentry.withScope"
    - path: "src/app/api/admin/purchases/[id]/process/route.ts"
      provides: "Sentry.withScope in else block (prod inngest.send failure)"
      contains: "Sentry.withScope"
  key_links:
    - from: "src/lib/inngest/process-search.ts"
      to: "@sentry/nextjs"
      via: "import * as Sentry + withScope in catch"
      pattern: "import \\* as Sentry from '@sentry/nextjs'"
    - from: "src/app/api/webhooks/abacatepay/route.ts"
      to: "@sentry/nextjs"
      via: "import * as Sentry + withScope in catch"
      pattern: "Sentry\\.withScope"
---

<objective>
Add structured Sentry error context to 5 critical points in the codebase so every error in the pipeline or API routes includes enough context (purchase_code, userId, pipeline_step, document_type, error_category) to identify the affected purchase without opening another system.

Purpose: Operador pode ir de um erro no Sentry direto para a compra afetada no admin panel usando purchase_code como tag pesquisavel.
Output: 5 instrumentation points with withScope, unit tests validating context structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sentry-com-contexto/02-CONTEXT.md
@.planning/phases/02-sentry-com-contexto/02-RESEARCH.md

@sentry.server.config.ts
@sentry.client.config.ts
@instrumentation.ts
@src/lib/inngest/process-search.ts
@src/app/api/webhooks/abacatepay/route.ts
@src/app/api/purchases/route.ts
@src/app/api/admin/purchases/[id]/process/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Sentry context unit tests</name>
  <files>tests/unit/sentry-context.test.ts</files>
  <read_first>
    - src/lib/inngest/process-search.ts (understand catch block structure, available variables)
    - src/app/api/webhooks/abacatepay/route.ts (understand catch block, purchaseCode availability)
    - src/app/api/purchases/route.ts (understand outer catch, existing Sentry.captureException at ~line 341)
    - src/app/api/admin/purchases/[id]/process/route.ts (understand else block ~line 79)
    - sentry.server.config.ts (understand current config)
  </read_first>
  <behavior>
    - Test 1: process-search pipeline error — withScope called with tags: error_category='pipeline', purchase_code, pipeline_step, document_type; user set with userId; extras: purchase_id, processing_step_number
    - Test 2: process-search INSUFFICIENT_API_BALANCE — withScope called with tags: error_category='infra', infra_type='apifull_balance'; extra: detail containing balance amount
    - Test 3: webhook catch — withScope called with tags: error_category='pipeline', purchase_code (or 'unknown' if unavailable)
    - Test 4: purchases outer catch — withScope called with available context (document_type, userId from session if available)
    - Test 5: admin process inngest.send failure — withScope called with tags: error_category='infra', infra_type='inngest_unreachable'; extras: detail
    - Test 6: No CPF/CNPJ (term) ever passed to any Sentry call (LGPD per D-04)
  </behavior>
  <action>
    Create `tests/unit/sentry-context.test.ts` using `vi.mock('@sentry/nextjs')` to mock the Sentry SDK. For each of the 5 instrumentation points, write a test that:

    1. Mocks `@sentry/nextjs` with `withScope` that captures the callback's scope operations and `captureException`
    2. Mocks Prisma and other dependencies as needed to simulate the error path
    3. Triggers the error condition
    4. Asserts `withScope` was called exactly once
    5. Asserts the scope had correct `setTag`, `setUser`, `setExtra` calls per D-03 structure
    6. Asserts `captureException` was called with the error

    For the LGPD test (Test 6): grep through all Sentry-related calls in tests to ensure `term` (CPF/CNPJ) is never included in any tag, extra, or user field.

    Use `import { describe, it, expect, vi, beforeEach } from 'vitest'`.
    Mock pattern: `vi.mock('@sentry/nextjs', () => ({ withScope: vi.fn((cb) => cb(mockScope)), captureException: vi.fn() }))` where `mockScope` has `setTag`, `setUser`, `setExtra` as `vi.fn()`.

    NOTE: The exact test structure depends on whether instrumentation is inline in catch blocks or extracted to helpers. Write tests that exercise the Sentry calls regardless — if inline, tests will need to mock the broader function context (Prisma queries, etc). If helpers are extracted in Task 2, tests can target the helpers directly. Either way, tests must verify the tag/extra structure per D-03.
  </action>
  <verify>
    <automated>cd "/Users/luana/Documents/Code Projects/eopix" && npx vitest run tests/unit/sentry-context.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/sentry-context.test.ts` exists with at least 6 test cases
    - `grep -c "withScope" tests/unit/sentry-context.test.ts` returns >= 5
    - `grep -c "error_category" tests/unit/sentry-context.test.ts` returns >= 5
    - `grep -c "captureException" tests/unit/sentry-context.test.ts` returns >= 5
    - `grep "term" tests/unit/sentry-context.test.ts` does NOT show term being passed to setTag/setExtra/setUser (LGPD)
    - Tests run RED (fail) before Task 2 implementation
  </acceptance_criteria>
  <done>6+ test cases covering all 5 instrumentation points plus LGPD check, all initially failing (RED)</done>
</task>

<task type="auto">
  <name>Task 2: Instrument 5 Sentry capture points with structured context</name>
  <files>
    src/lib/inngest/process-search.ts
    src/app/api/webhooks/abacatepay/route.ts
    src/app/api/purchases/route.ts
    src/app/api/admin/purchases/[id]/process/route.ts
  </files>
  <read_first>
    - src/lib/inngest/process-search.ts (full file — understand catch block at ~line 366, check-balance at ~line 128, verify event.data fields including purchaseCode)
    - src/app/api/webhooks/abacatepay/route.ts (full file — understand catch block at ~line 154, purchaseCode variable scope)
    - src/app/api/purchases/route.ts (full file — understand outer catch at ~line 353, existing Sentry import and captureException at ~line 341)
    - src/app/api/admin/purchases/[id]/process/route.ts (full file — understand catch block ~line 69, else block ~line 79)
    - tests/unit/sentry-context.test.ts (understand what the tests expect)
    - sentry.server.config.ts (verify current config)
  </read_first>
  <action>
    Instrument all 5 points per D-01 and D-03. Use `import * as Sentry from '@sentry/nextjs'` (static import, server-side). Use `Sentry.withScope()` to isolate context per D-03. NO guards needed around capture calls (SDK is no-op without init per D-07).

    **Point 1 — process-search.ts catch block final (~line 366):**
    - Add `purchaseCode` to the destructuring of `event.data` (verify it exists — webhook sends it as `purchaseCode: purchase.code`)
    - Expand the `failedPurchase` query select to include `code: true, userId: true` (currently only has `processingStep: true`)
    - In the catch block, BEFORE `throw error`, detect if `error.message?.includes('INSUFFICIENT_API_BALANCE')`:
      - If YES (Point 4 — systemic): `Sentry.withScope(scope => { scope.setTag('error_category', 'infra'); scope.setTag('infra_type', 'apifull_balance'); scope.setExtra('detail', error instanceof Error ? error.message : String(error)); Sentry.captureException(error) })`
      - If NO (Point 1 — per-purchase): `Sentry.withScope(scope => { scope.setUser({ id: failedPurchase?.userId ?? 'unknown' }); scope.setTag('error_category', 'pipeline'); scope.setTag('purchase_code', failedPurchase?.code ?? purchaseCode ?? 'unknown'); scope.setTag('pipeline_step', String(failedPurchase?.processingStep ?? 'unknown')); scope.setTag('document_type', type); scope.setExtra('purchase_id', purchaseId); scope.setExtra('processing_step_number', failedPurchase?.processingStep ?? null); Sentry.captureException(error) })`
    - Add `import * as Sentry from '@sentry/nextjs'` at top of file

    **Point 2 — webhooks/abacatepay/route.ts catch block (~line 154):**
    - Add `import * as Sentry from '@sentry/nextjs'` at top
    - In the catch block, before the return statement: `Sentry.withScope(scope => { scope.setTag('error_category', 'pipeline'); scope.setTag('purchase_code', purchaseCode ?? 'unknown'); Sentry.captureException(error instanceof Error ? error : new Error(String(error))) })`
    - Ensure `purchaseCode` variable is declared with `let purchaseCode: string | null = null` BEFORE the try block if not already, so it survives into catch

    **Point 3 — purchases/route.ts outer catch (~line 353):**
    - Sentry is already imported in this file
    - In the outer catch block, add: `Sentry.withScope(scope => { if (session?.userId) scope.setUser({ id: session.userId }); scope.setTag('error_category', 'pipeline'); if (purchase?.code) scope.setTag('purchase_code', purchase.code); scope.setTag('document_type', cleanedTerm?.length === 11 ? 'CPF' : cleanedTerm?.length === 14 ? 'CNPJ' : 'unknown'); Sentry.captureException(error instanceof Error ? error : new Error(String(error))) })`
    - Ensure variables `session`, `purchase`, `cleanedTerm` are accessible in catch scope (declare with `let` before try if needed)

    **Point 5 — admin/purchases/[id]/process/route.ts else block (~line 79):**
    - Add `import * as Sentry from '@sentry/nextjs'` at top
    - In the else block (production path, after rollback to PAID), before the return: `Sentry.withScope(scope => { scope.setTag('error_category', 'infra'); scope.setTag('infra_type', 'inngest_unreachable'); scope.setTag('purchase_code', purchase.code); scope.setExtra('detail', err instanceof Error ? err.message : String(err)); Sentry.captureException(err instanceof Error ? err : new Error(String(err))) })`

    **CRITICAL per D-04:** NEVER pass `term`, `cleanedTerm`, or any CPF/CNPJ value to Sentry tags, extras, or user fields. Only `purchase_code` for lookup.
  </action>
  <verify>
    <automated>cd "/Users/luana/Documents/Code Projects/eopix" && npx vitest run tests/unit/sentry-context.test.ts -x && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Sentry.withScope" src/lib/inngest/process-search.ts` returns >= 1
    - `grep -c "Sentry.withScope" src/app/api/webhooks/abacatepay/route.ts` returns >= 1
    - `grep -c "Sentry.withScope" src/app/api/purchases/route.ts` returns >= 1
    - `grep -c "Sentry.withScope" "src/app/api/admin/purchases/[id]/process/route.ts"` returns >= 1
    - `grep "error_category.*pipeline" src/lib/inngest/process-search.ts` matches
    - `grep "error_category.*infra" src/lib/inngest/process-search.ts` matches
    - `grep "infra_type.*inngest_unreachable" "src/app/api/admin/purchases/[id]/process/route.ts"` matches
    - `grep -rn "setTag.*term\|setExtra.*term\|setUser.*term" src/lib/inngest/process-search.ts src/app/api/webhooks/abacatepay/route.ts src/app/api/purchases/route.ts "src/app/api/admin/purchases/[id]/process/route.ts"` returns NO matches (LGPD D-04)
    - `npx vitest run tests/unit/sentry-context.test.ts` all tests pass (GREEN)
    - `npx vitest run` full suite passes
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>All 5 instrumentation points have Sentry.withScope with structured context per D-03. Unit tests pass. Full test suite passes. No CPF/CNPJ data in Sentry calls. tsc clean.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/unit/sentry-context.test.ts -x` — all 6+ tests pass
2. `npx vitest run` — full suite green (no regressions)
3. `npx tsc --noEmit` — no type errors
4. `grep -rn "Sentry.withScope" src/lib/inngest/process-search.ts src/app/api/webhooks/abacatepay/route.ts src/app/api/purchases/route.ts "src/app/api/admin/purchases/[id]/process/route.ts"` — 5 matches across 4 files
5. `grep -rn "setTag.*term\b\|setExtra.*term\b" src/` — no CPF/CNPJ leaks (LGPD D-04)
6. OBS-04: Manual verification — user must configure `NEXT_PUBLIC_SENTRY_DSN` on Vercel and confirm a test error appears in Sentry dashboard
</verification>

<success_criteria>
- 5 Sentry instrumentation points with withScope and structured tags (per D-01, D-03)
- Pipeline errors tagged error_category:pipeline with purchase_code, pipeline_step, document_type, userId
- Systemic errors tagged error_category:infra with infra_type
- Zero CPF/CNPJ data in Sentry (per D-04, LGPD)
- Unit tests covering all 5 points + LGPD check
- Full test suite and tsc pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-sentry-com-contexto/02-01-SUMMARY.md`
</output>
