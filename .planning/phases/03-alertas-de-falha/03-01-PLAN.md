---
phase: 03-alertas-de-falha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/callmebot.ts
  - src/lib/inngest/process-search.ts
  - tests/lib/callmebot.test.ts
  - .claude/rules/callmebot.md
  - docs/status.md
autonomous: true
requirements: [OBS-03]
user_setup:
  - service: callmebot
    why: "WhatsApp alert delivery"
    env_vars:
      - name: CALLMEBOT_PHONE
        source: "Phone number with country code (e.g. +5511999999999)"
      - name: CALLMEBOT_API_KEY
        source: "Received after sending 'I allow callmebot to send me messages' to Callmebot WhatsApp"
      - name: CALLMEBOT_PHONE_2
        source: "Kevin's phone (same activation process)"
      - name: CALLMEBOT_API_KEY_2
        source: "Kevin's API key"
      - name: CALLMEBOT_PHONE_3
        source: "Carolina's phone (same activation process)"
      - name: CALLMEBOT_API_KEY_3
        source: "Carolina's API key"
    dashboard_config:
      - task: "Each recipient must send 'I allow callmebot to send me messages' to +34 611 04 87 48 on WhatsApp"
        location: "WhatsApp"

must_haves:
  truths:
    - "Operador recebe WhatsApp quando purchase transiciona para FAILED pela primeira vez"
    - "Operador recebe WhatsApp quando purchase transiciona para COMPLETED"
    - "Retries do Inngest nao geram alertas duplicados de FAILED"
    - "PAYMENT_EXPIRED nao gera alerta"
    - "Falha do Callmebot nao bloqueia o pipeline"
  artifacts:
    - path: "src/lib/callmebot.ts"
      provides: "sendFailureAlert, sendCompletedAlert, broadcastAlert"
      exports: ["sendFailureAlert", "sendCompletedAlert"]
    - path: "tests/lib/callmebot.test.ts"
      provides: "Unit tests for callmebot module"
      min_lines: 50
  key_links:
    - from: "src/lib/inngest/process-search.ts"
      to: "src/lib/callmebot.ts"
      via: "import { sendFailureAlert, sendCompletedAlert }"
      pattern: "sendFailureAlert|sendCompletedAlert"
    - from: "src/lib/callmebot.ts"
      to: "https://api.callmebot.com/whatsapp.php"
      via: "fetch GET"
      pattern: "api\\.callmebot\\.com"
---

<objective>
Adicionar alertas WhatsApp via Callmebot quando purchases transitam para FAILED (primeira vez) ou COMPLETED no pipeline Inngest.

Purpose: Operador sabe imediatamente quando algo falha ou quando um relatorio e entregue, sem monitorar dashboards.
Output: Modulo callmebot.ts + integracao em process-search.ts + testes unitarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-alertas-de-falha/03-CONTEXT.md
@.planning/phases/03-alertas-de-falha/03-RESEARCH.md
@src/lib/inngest/process-search.ts
@src/lib/email.ts
@src/types/domain.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create callmebot.ts module + unit tests + rule file</name>
  <files>src/lib/callmebot.ts, tests/lib/callmebot.test.ts, .claude/rules/callmebot.md</files>
  <read_first>
    - src/lib/email.ts (pattern to follow for module structure, env guards, fire-and-forget)
    - src/types/domain.ts (PROCESSING_STEPS array for step label lookup)
    - .planning/phases/03-alertas-de-falha/03-RESEARCH.md (complete module code example in "Code Examples" section)
  </read_first>
  <behavior>
    - sendFailureAlert builds message with: warning emoji header "FALHA NO PIPELINE", code, formatted date (DD/MM/YYYY as HHhMM, America/Sao_Paulo), user name (fallback "Sem nome"), email, paymentProvider mapped ("abacatepay"->"PIX (AbacatePay)", "stripe"->"Cartao (Stripe)", null->"Pagamento"), step X/6 with label from PROCESSING_STEPS, error truncated at 100 chars with "...", admin link
    - sendCompletedAlert builds message with: checkmark emoji header "RELATORIO ENTREGUE", code, formatted date, user name, email, paymentProvider mapped, admin link
    - broadcastAlert sends to 3 recipients in parallel via Promise.all using CALLMEBOT_PHONE/API_KEY, CALLMEBOT_PHONE_2/API_KEY_2, CALLMEBOT_PHONE_3/API_KEY_3
    - Recipients with missing phone or apiKey env var are skipped silently
    - If zero recipients configured, logs warning and returns (no fetch calls)
    - Each recipient fetch failure is caught individually (Sentry.captureException + console.error), does not block other recipients
    - Error message longer than 100 chars is truncated with "..." appended
    - Error message exactly 100 chars or shorter is NOT truncated
    - fetch URL uses encodeURIComponent for phone, text, and apikey params
  </behavior>
  <action>
    Create `src/lib/callmebot.ts` following the complete code example in RESEARCH.md "Code Examples" section. Key details:

    1. BASE_URL = `https://api.callmebot.com/whatsapp.php`
    2. `formatDate(date: Date)`: use `toLocaleDateString('pt-BR', { day:'2-digit', month:'2-digit', year:'numeric', hour:'2-digit', minute:'2-digit', timeZone:'America/Sao_Paulo' })` then `.replace(',', ' as').replace(':', 'h')`
    3. `mapPaymentProvider(provider: string | null)`: `abacatepay` -> `PIX (AbacatePay)`, `stripe` -> `Cartao (Stripe)`, default -> `Pagamento`
    4. `getStepLabel(step: number)`: `PROCESSING_STEPS.find(s => s.step === step)?.label ?? 'Iniciando'` — import from `@/types/domain`
    5. `sendToOne(phone, apiKey, message)`: fetch GET with all 3 params URL-encoded. Throw on non-ok response.
    6. `broadcastAlert(message)`: build recipients array from 3 env var pairs, filter where both phone and apiKey present, if empty log warn and return, Promise.all with per-recipient .catch that does console.error + Sentry.captureException
    7. Export `sendFailureAlert(payload: FailureAlertPayload)` and `sendCompletedAlert(payload: CompletedAlertPayload)` — both call broadcastAlert with formatted message string joined by `\n`
    8. Import `* as Sentry from '@sentry/nextjs'`

    FailureAlertPayload: `{ code: string, createdAt: Date, userName: string | null, userEmail: string, paymentProvider: string | null, processingStep: number, errorMessage: string }`
    CompletedAlertPayload: `{ code: string, createdAt: Date, userName: string | null, userEmail: string, paymentProvider: string | null }`

    FAILED message format (per D-04):
    ```
    ⚠️ EOPIX — FALHA NO PIPELINE

    📦 Compra: {code}
    📅 Data: {formatted date}
    👤 {userName || 'Sem nome'}
    📧 {userEmail}
    💳 {mapped provider}
    🔢 Step: {step}/6 — {stepLabel}

    ❌ Erro:
    {truncated error}

    🔗 eopix.com.br/admin/compras?search={code}
    ```

    COMPLETED message format (per D-08):
    ```
    ✅ EOPIX — RELATÓRIO ENTREGUE

    📦 Compra: {code}
    📅 Data: {formatted date}
    👤 {userName || 'Sem nome'}
    📧 {userEmail}
    💳 {mapped provider}

    🔗 eopix.com.br/admin/compras?search={code}
    ```

    Create `tests/lib/callmebot.test.ts` following pattern of existing test files. Use `vi.hoisted` for fetch mock, `vi.mock('@sentry/nextjs')`. Test cases:
    - sendFailureAlert constructs correct message (check key substrings in fetch call's text param)
    - sendCompletedAlert constructs correct message
    - Error > 100 chars is truncated with "..."
    - Error <= 100 chars is NOT truncated
    - No env vars configured -> no fetch calls, console.warn called
    - One recipient fails -> others still called (mock first fetch to reject, verify second/third still called)
    - paymentProvider mapping: abacatepay/stripe/null
    - Step label lookup from PROCESSING_STEPS

    Per CLAUDE.md rule "Se criar novo modulo em `src/lib/`: criar rule correspondente em `.claude/rules/`", create `.claude/rules/callmebot.md` with minimal content:
    - Globs: `src/lib/callmebot.ts`
    - Notes: Callmebot WhatsApp alert module. Fire-and-forget pattern. 3 recipients via env vars (CALLMEBOT_PHONE/API_KEY, _2, _3). Uses Sentry for error capture. After editing, update docs/status.md.
  </action>
  <verify>
    <automated>cd "/Users/luana/Documents/Code Projects/eopix" && npx vitest run tests/lib/callmebot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "sendFailureAlert" src/lib/callmebot.ts
    - grep -q "sendCompletedAlert" src/lib/callmebot.ts
    - grep -q "broadcastAlert" src/lib/callmebot.ts
    - grep -q "api.callmebot.com/whatsapp.php" src/lib/callmebot.ts
    - grep -q "CALLMEBOT_PHONE_2" src/lib/callmebot.ts
    - grep -q "CALLMEBOT_PHONE_3" src/lib/callmebot.ts
    - grep -q "encodeURIComponent" src/lib/callmebot.ts
    - grep -q "Promise.all" src/lib/callmebot.ts
    - grep -q "Sentry.captureException" src/lib/callmebot.ts
    - grep -q "PROCESSING_STEPS" src/lib/callmebot.ts
    - grep -q "sendFailureAlert" tests/lib/callmebot.test.ts
    - grep -q "sendCompletedAlert" tests/lib/callmebot.test.ts
    - test $(grep -c "it\|test(" tests/lib/callmebot.test.ts) -ge 7
    - test -f .claude/rules/callmebot.md
  </acceptance_criteria>
  <done>callmebot.ts exports sendFailureAlert and sendCompletedAlert. All unit tests pass. Module handles 3 recipients, env guard, error truncation, fire-and-forget pattern. Rule file created at .claude/rules/callmebot.md.</done>
</task>

<task type="auto">
  <name>Task 2: Wire callmebot alerts into process-search.ts + update docs/status.md</name>
  <files>src/lib/inngest/process-search.ts, docs/status.md</files>
  <read_first>
    - src/lib/inngest/process-search.ts (full file — find catch block ~line 376 for FAILED integration, and the COMPLETED update point)
    - src/lib/callmebot.ts (just created — import paths and payload types)
    - .planning/phases/03-alertas-de-falha/03-CONTEXT.md (D-02 deduplication pattern, D-09 COMPLETED fire point, D-11 PAYMENT_EXPIRED exclusion)
  </read_first>
  <action>
    Two integration points in `src/lib/inngest/process-search.ts`:

    **Import at top:**
    ```typescript
    import { sendFailureAlert, sendCompletedAlert } from '@/lib/callmebot'
    ```

    **FAILED alert (catch block):**
    1. Find the existing `prisma.purchase.findUnique` in the catch block. Expand its `select` to include: `status: true`, `createdAt: true`, `paymentProvider: true`, `failureReason: true`, `user: { select: { name: true, email: true } }` (keep existing fields: `processingStep`, `code`, `userId`)
    2. Before the existing `prisma.purchase.update({ status: 'FAILED' })`, capture: `const wasAlreadyFailed = failedPurchase?.status === 'FAILED'`
    3. After the existing update (before the email send), add:
    ```typescript
    if (!wasAlreadyFailed && failedPurchase?.failureReason !== 'PAYMENT_EXPIRED') {
      sendFailureAlert({
        code: failedPurchase?.code ?? purchaseCode ?? 'UNKNOWN',
        createdAt: failedPurchase?.createdAt ?? new Date(),
        userName: failedPurchase?.user?.name ?? null,
        userEmail: failedPurchase?.user?.email ?? 'desconhecido',
        paymentProvider: failedPurchase?.paymentProvider ?? null,
        processingStep: failedPurchase?.processingStep ?? 0,
        errorMessage: error instanceof Error ? error.message : String(error),
      }).catch(err => {
        console.error('[Callmebot] Failure alert failed:', err)
        Sentry.captureException(err)
      })
    }
    ```

    **COMPLETED alert:**
    1. Find `prisma.purchase.update({ ... status: 'COMPLETED' ... })` in the success path
    2. Immediately after that update, add fire-and-forget alert. The purchase data needed (code, createdAt, paymentProvider, user name/email) should be available from the scope or from a preceding query. If `createdAt` or `paymentProvider` are not in scope, expand the nearest preceding `findUnique`/`select` to include them, or use the result of the COMPLETED update itself.
    ```typescript
    sendCompletedAlert({
      code: purchaseCode,
      createdAt: /* from scope or query */,
      userName: /* from scope or query */,
      userEmail: /* from scope or query */,
      paymentProvider: /* from scope or query */,
    }).catch(err => {
      console.error('[Callmebot] Completed alert failed:', err)
      Sentry.captureException(err)
    })
    ```

    Do NOT await the alert calls — they are fire-and-forget per D-07.
    Do NOT send alert when `failureReason === 'PAYMENT_EXPIRED'` per D-11.

    **Update docs/status.md:**
    Add entry under "O que esta funcionando" section:
    - **Alertas WhatsApp (Callmebot)** — FAILED (primeira vez, exclui PAYMENT_EXPIRED) e COMPLETED disparam alerta WhatsApp via Callmebot para 3 destinatarios. Fire-and-forget, falha do alerta nao bloqueia pipeline. Deduplicacao via wasAlreadyFailed.

    Add entry under "Ultimas mudancas" section with current date:
    - **Alertas WhatsApp via Callmebot** (2026-03-XX): `src/lib/callmebot.ts` com sendFailureAlert e sendCompletedAlert. 3 destinatarios via env vars (CALLMEBOT_PHONE/API_KEY, _2, _3). Integrado em `process-search.ts`: FAILED (primeira transicao, exclui PAYMENT_EXPIRED) e COMPLETED. Fire-and-forget com Sentry. Rule file `.claude/rules/callmebot.md`.
  </action>
  <verify>
    <automated>cd "/Users/luana/Documents/Code Projects/eopix" && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "sendFailureAlert" src/lib/inngest/process-search.ts
    - grep -q "sendCompletedAlert" src/lib/inngest/process-search.ts
    - grep -q "wasAlreadyFailed" src/lib/inngest/process-search.ts
    - grep -q "PAYMENT_EXPIRED" src/lib/inngest/process-search.ts
    - grep -q "import.*callmebot" src/lib/inngest/process-search.ts
    - grep -q "paymentProvider" src/lib/inngest/process-search.ts
    - grep -q "Callmebot" docs/status.md
  </acceptance_criteria>
  <done>process-search.ts sends FAILED alert on first transition (deduplicated via wasAlreadyFailed), skips PAYMENT_EXPIRED, sends COMPLETED alert after success update. Both fire-and-forget. All vitest tests pass. tsc clean. docs/status.md updated.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass including new callmebot tests
2. `npx tsc --noEmit` — no type errors
3. `npm run lint` — no lint errors
4. grep confirms both alert functions wired in process-search.ts
5. `.claude/rules/callmebot.md` exists
6. `docs/status.md` mentions Callmebot
</verification>

<success_criteria>
- callmebot.ts module exists with sendFailureAlert and sendCompletedAlert exports
- process-search.ts imports and calls both alert functions at correct points
- FAILED deduplication via wasAlreadyFailed status check (no DB migration)
- PAYMENT_EXPIRED excluded from alerts
- 3 recipients via Promise.all, missing env vars skipped silently
- Fire-and-forget pattern (errors caught, logged, Sentry captured, never thrown)
- All vitest tests pass, tsc clean
- .claude/rules/callmebot.md exists with module guidance
- docs/status.md updated with Callmebot entry
</success_criteria>

<output>
After completion, create `.planning/phases/03-alertas-de-falha/03-01-SUMMARY.md`
</output>
