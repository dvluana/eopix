---
phase: 04-pix-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/abacatepay.ts
  - src/app/api/purchases/pix/route.ts
  - src/app/api/purchases/pix/status/route.ts
  - src/app/api/webhooks/abacatepay/route.ts
  - prisma/schema.prisma
  - tests/lib/abacatepay-pix.test.ts
  - tests/unit/webhook-transparent.test.ts
  - docs/api-contracts/pix-transparent.md
  - docs/status.md
autonomous: true
requirements: [PIX-01, PIX-04]

must_haves:
  truths:
    - "POST /api/purchases/pix creates a PIX charge via AbacatePay and returns brCode + brCodeBase64"
    - "GET /api/purchases/pix/status returns current PIX status from AbacatePay for PENDING purchases"
    - "Webhook transparent.completed advances purchase to PAID and triggers Inngest"
    - "Duplicate transparent.completed webhooks are ignored via WebhookLog idempotency"
    - "Bypass mode returns mock PIX data without calling AbacatePay"
  artifacts:
    - path: "src/lib/abacatepay.ts"
      provides: "createPixCharge, checkPixStatus, simulatePixPayment functions"
      contains: "createPixCharge"
    - path: "src/app/api/purchases/pix/route.ts"
      provides: "POST handler for PIX charge creation"
      exports: ["POST"]
    - path: "src/app/api/purchases/pix/status/route.ts"
      provides: "GET handler for PIX status polling"
      exports: ["GET"]
    - path: "src/app/api/webhooks/abacatepay/route.ts"
      provides: "transparent.completed webhook handling"
      contains: "transparent.completed"
  key_links:
    - from: "src/app/api/purchases/pix/route.ts"
      to: "src/lib/abacatepay.ts"
      via: "createPixCharge call"
      pattern: "createPixCharge"
    - from: "src/app/api/webhooks/abacatepay/route.ts"
      to: "handlePaymentSuccess"
      via: "transparent.completed event branch"
      pattern: "transparent\\.completed"
    - from: "src/app/api/purchases/pix/status/route.ts"
      to: "src/lib/abacatepay.ts"
      via: "checkPixStatus call"
      pattern: "checkPixStatus"
---

<objective>
Add transparent PIX checkout backend: lib functions for AbacatePay PIX QR Code API, two new API routes (create charge + poll status), and webhook extension for transparent.completed events.

Purpose: Enable Phase 5 (frontend) to display inline PIX QR codes and auto-detect payment without redirecting to AbacatePay.
Output: Working PIX backend with unit tests and API contract documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pix-backend/04-RESEARCH.md

@src/lib/abacatepay.ts
@src/app/api/webhooks/abacatepay/route.ts
@src/lib/mock-mode.ts
@prisma/schema.prisma
@src/types/domain.ts

<interfaces>
From src/lib/abacatepay.ts:
```typescript
export function createCheckout(params: CreateCheckoutParams): Promise<CheckoutResponse>
export function validateWebhookSecret(url: string): boolean
export function validateWebhookSignature(rawBody: string, signatureHeader: string): boolean
```

From src/lib/mock-mode.ts:
```typescript
export const isBypassPayment: boolean  // true when payment should be skipped
export const isMockMode: boolean
```

From src/app/api/webhooks/abacatepay/route.ts:
```typescript
// handlePaymentSuccess is a local function (not exported) — reuse by extending the webhook handler inline
async function handlePaymentSuccess(purchaseCode: string, checkoutId: string, customerEmail: string | null, customerName: string | null): void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PIX lib functions + Prisma migration + unit tests</name>
  <files>
    src/lib/abacatepay.ts
    prisma/schema.prisma
    tests/lib/abacatepay-pix.test.ts
  </files>
  <read_first>
    src/lib/abacatepay.ts
    src/lib/mock-mode.ts
    prisma/schema.prisma
    .planning/phases/04-pix-backend/04-RESEARCH.md (Code Examples section)
    tests/lib/purchase-workflow.test.ts (test pattern reference)
  </read_first>
  <behavior>
    - createPixCharge: calls POST /v1/pixQrCode/create with amount, expiresIn, description, metadata.externalId, optional customer; returns { pixId, brCode, brCodeBase64, expiresAt, status }
    - createPixCharge bypass: when isBypassPayment is true, returns mock data with pixId starting "pix_bypass_" without calling fetch
    - createPixCharge error: throws Error when API returns non-ok or missing data.id
    - checkPixStatus: calls GET /v1/pixQrCode/check?id=xxx; returns { status, expiresAt }
    - checkPixStatus error: throws when API returns non-ok
    - simulatePixPayment: calls POST /v1/pixQrCode/simulate-payment?id=xxx with empty body
  </behavior>
  <action>
    1. Add Prisma migration for PIX data storage (needed because AbacatePay status check does NOT return brCode):
       - Add nullable `pixBrCode TEXT` column to Purchase (stores PIX copia-e-cola string)
       - Add nullable `pixBrCodeBase64 TEXT` column to Purchase (stores QR code base64 image)
       - Add nullable `pixExpiresAt TIMESTAMP` column to Purchase
       - Run `npx prisma migrate dev --name add-pix-fields`

    2. Add to `src/lib/abacatepay.ts` (after existing exports):

       Export interface `CreatePixChargeParams` with fields:
       - `purchaseCode: string` (stored in metadata.externalId)
       - `amount: number` (centavos)
       - `customer?: { name: string; email: string; taxId: string; cellphone: string }`
       - `expiresIn?: number` (seconds, default 3600)

       Export interface `PixChargeResponse` with fields:
       - `pixId: string`
       - `brCode: string`
       - `brCodeBase64: string`
       - `expiresAt: string`
       - `status: 'PENDING' | 'PAID' | 'EXPIRED' | 'CANCELLED' | 'REFUNDED'`

       Export async function `createPixCharge(params: CreatePixChargeParams): Promise<PixChargeResponse>`:
       - If `isBypassPayment`: return mock with `pixId: "pix_bypass_${Date.now()}"`, `brCode: 'BYPASS_BR_CODE'`, `brCodeBase64: 'data:image/png;base64,BYPASS'`, `expiresAt` 1h from now, `status: 'PENDING'`
       - Else: fetch POST `https://api.abacatepay.com/v1/pixQrCode/create` with Bearer auth, body `{ amount, expiresIn: params.expiresIn ?? 3600, description: 'Relatório EOPIX', metadata: { externalId: params.purchaseCode }, ...(params.customer ? { customer: params.customer } : {}) }`
       - On !res.ok or !data.data?.id: throw Error with message from data.error or res.status
       - Return mapped response from data.data fields

       Export async function `checkPixStatus(pixId: string): Promise<{ status: string; expiresAt: string }>`:
       - fetch GET `https://api.abacatepay.com/v1/pixQrCode/check?id=${pixId}` with Bearer auth
       - On !res.ok: throw Error
       - Return `{ status: data.data.status, expiresAt: data.data.expiresAt }`

       Export async function `simulatePixPayment(pixId: string): Promise<void>`:
       - fetch POST `https://api.abacatepay.com/v1/pixQrCode/simulate-payment?id=${pixId}` with Bearer auth and empty JSON body
       - No return value needed

    3. Write tests in `tests/lib/abacatepay-pix.test.ts`:
       - Mock global.fetch with vi.fn()
       - Mock `isBypassPayment` from `@/lib/mock-mode`
       - Test createPixCharge: correct URL, method, headers, body structure, response mapping
       - Test createPixCharge bypass: returns mock without calling fetch
       - Test createPixCharge error: throws on non-ok response
       - Test checkPixStatus: correct URL, response mapping
       - Test simulatePixPayment: correct URL and method
  </action>
  <verify>
    <automated>cd /Users/luana/Documents/Code\ Projects/eopix && npx vitest run tests/lib/abacatepay-pix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "createPixCharge" src/lib/abacatepay.ts
    - grep -q "checkPixStatus" src/lib/abacatepay.ts
    - grep -q "simulatePixPayment" src/lib/abacatepay.ts
    - grep -q "pixBrCode" prisma/schema.prisma
    - grep -q "pixBrCodeBase64" prisma/schema.prisma
    - grep -q "pixExpiresAt" prisma/schema.prisma
    - grep -q "v1/pixQrCode/create" src/lib/abacatepay.ts
    - grep -q "v1/pixQrCode/check" src/lib/abacatepay.ts
    - test -f tests/lib/abacatepay-pix.test.ts
  </acceptance_criteria>
  <done>
    createPixCharge, checkPixStatus, simulatePixPayment exported from abacatepay.ts. Prisma schema has pixBrCode, pixBrCodeBase64, pixExpiresAt on Purchase. All unit tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PIX API routes + webhook extension + tests + API contract doc + status update</name>
  <files>
    src/app/api/purchases/pix/route.ts
    src/app/api/purchases/pix/status/route.ts
    src/app/api/webhooks/abacatepay/route.ts
    tests/unit/webhook-transparent.test.ts
    docs/api-contracts/pix-transparent.md
    docs/status.md
  </files>
  <read_first>
    src/lib/abacatepay.ts (after Task 1 modifications)
    src/app/api/webhooks/abacatepay/route.ts
    src/app/api/purchases/route.ts (existing purchase creation pattern — auth, validation, Zod)
    src/lib/auth.ts (getSession pattern)
    .planning/phases/04-pix-backend/04-RESEARCH.md (Pattern 4: Webhook Extension)
  </read_first>
  <behavior>
    - POST /api/purchases/pix: requires authenticated session, accepts { purchaseId: string }, finds PENDING purchase owned by user, calls createPixCharge, stores pixId/brCode/brCodeBase64/pixExpiresAt in purchase, returns { pixId, brCode, brCodeBase64, expiresAt }
    - POST /api/purchases/pix: reuses existing PIX charge if purchase.paymentExternalId starts with "pix_" and purchase has pixBrCode (avoids double creation on refresh)
    - POST /api/purchases/pix: returns 401 without session, 404 if purchase not found or not owned, 400 if purchase not PENDING
    - GET /api/purchases/pix/status: requires purchaseId query param, returns DB status for COMPLETED/FAILED, calls checkPixStatus for PENDING purchases, returns { status, expiresAt }
    - Webhook transparent.completed: extracts purchase code from event.data.transparent.metadata?.externalId, falls back to DB lookup by paymentExternalId, calls handlePaymentSuccess, logs to WebhookLog with key "abacate:transparent:{id}"
    - Webhook transparent.completed: duplicate ignored via WebhookLog
  </behavior>
  <action>
    1. Create `src/app/api/purchases/pix/route.ts`:
       - Import `getSession` from `@/lib/auth`, `prisma` from `@/lib/prisma`, `createPixCharge` from `@/lib/abacatepay`, `Sentry` from `@sentry/nextjs`, `z` from `zod`
       - Zod schema: `{ purchaseId: z.string() }`
       - POST handler:
         a. Get session — 401 if missing
         b. Parse body with Zod — 400 on failure
         c. Find purchase by id where `userId = session.userId` and status `PENDING` — 404 if not found, 400 if not PENDING
         d. If `purchase.paymentExternalId?.startsWith('pix_') && purchase.pixBrCode`: return existing `{ pixId: purchase.paymentExternalId, brCode: purchase.pixBrCode, brCodeBase64: purchase.pixBrCodeBase64, expiresAt: purchase.pixExpiresAt?.toISOString() }` (reuse on refresh)
         e. Call `createPixCharge({ purchaseCode: purchase.code, amount: purchase.amount, customer: { name: user.name || 'Cliente EOPIX', email: user.email, taxId: user.taxId || purchase.buyerCpfCnpj || purchase.term, cellphone: user.cellphone || '' } })`
         f. Update purchase: `paymentExternalId = result.pixId, pixBrCode = result.brCode, pixBrCodeBase64 = result.brCodeBase64, pixExpiresAt = new Date(result.expiresAt)`
         g. Return 200 with `{ pixId, brCode, brCodeBase64, expiresAt }`
         h. Wrap in try/catch — Sentry.captureException on error, return 500

    2. Create `src/app/api/purchases/pix/status/route.ts`:
       - GET handler with `searchParams.get('purchaseId')`
       - Get session — 401 if missing
       - Find purchase by id where `userId = session.userId` — 404 if not found
       - If purchase status is `COMPLETED` or `FAILED`: return `{ status: purchase.status, expiresAt: null }` from DB (no API call)
       - If purchase.paymentExternalId starts with `pix_`: call `checkPixStatus(purchase.paymentExternalId)`, return `{ status: pixStatus.status, expiresAt: pixStatus.expiresAt }`
       - Else: return `{ status: purchase.status, expiresAt: null }`

    3. Extend `src/app/api/webhooks/abacatepay/route.ts`:
       - Add `transparent?: { id: string; metadata?: { externalId?: string }; amount?: number; status?: string }` to `AbacateWebhookEvent.data` interface
       - Extend `isPaymentEvent` check: add `|| event.event === 'transparent.completed'`
       - After existing checkout/billing extraction, add transparent branch:
         ```
         const transparent = event.data.transparent
         ```
       - Update `checkoutId` extraction: `const checkoutId = checkout?.id || billing?.id || transparent?.id`
       - Update `webhookKey`: if `event.event === 'transparent.completed'`, use `abacate:transparent:${checkoutId}` (different namespace to avoid key collision with checkout for same purchase)
       - After existing purchaseCode extraction, add transparent branch:
         ```
         if (!purchaseCode && event.event === 'transparent.completed' && transparent) {
           purchaseCode = transparent.metadata?.externalId ?? null
           if (!purchaseCode && checkoutId) {
             const byPixId = await prisma.purchase.findFirst({ where: { paymentExternalId: checkoutId } })
             purchaseCode = byPixId?.code ?? null
           }
         }
         ```

    4. Write `tests/unit/webhook-transparent.test.ts`:
       - Test transparent.completed event advances purchase to PAID (mock prisma + inngest)
       - Test duplicate transparent.completed is ignored (WebhookLog exists)
       - Test transparent with metadata.externalId resolves purchase code
       - Test transparent fallback lookup by paymentExternalId
       - Pattern: follow `tests/unit/sentry-context.test.ts` mocking style

    5. Create `docs/api-contracts/pix-transparent.md`:
       - Document AbacatePay PIX QR Code API: create, check, simulate-payment
       - Note: uses `/v1/pixQrCode/*` paths (not v2)
       - Webhook event: `transparent.completed`
       - Include request/response examples

    6. Update `docs/status.md`:
       - Add entry under "O que esta funcionando" for PIX backend:
         "**PIX Transparent Checkout (backend)** — `createPixCharge`, `checkPixStatus`, `simulatePixPayment` in `abacatepay.ts`. API routes `POST /api/purchases/pix` (create charge) and `GET /api/purchases/pix/status` (poll). Webhook `transparent.completed` handled with idempotency. Prisma fields `pixBrCode`, `pixBrCodeBase64`, `pixExpiresAt` on Purchase."
       - Add entry under "Ultimas mudancas" with date and summary
  </action>
  <verify>
    <automated>cd /Users/luana/Documents/Code\ Projects/eopix && npx vitest run tests/unit/webhook-transparent.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/app/api/purchases/pix/route.ts
    - test -f src/app/api/purchases/pix/status/route.ts
    - grep -q "transparent.completed" src/app/api/webhooks/abacatepay/route.ts
    - grep -q "transparent" src/app/api/webhooks/abacatepay/route.ts
    - grep -q "createPixCharge" src/app/api/purchases/pix/route.ts
    - grep -q "checkPixStatus" src/app/api/purchases/pix/status/route.ts
    - test -f tests/unit/webhook-transparent.test.ts
    - test -f docs/api-contracts/pix-transparent.md
    - grep -q "PIX Transparent" docs/status.md
  </acceptance_criteria>
  <done>
    POST /api/purchases/pix creates PIX charge and returns brCode. GET /api/purchases/pix/status polls AbacatePay for PENDING purchases. Webhook handles transparent.completed with idempotency. docs/status.md updated with PIX backend entry. tsc passes. All tests green. Lint clean.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/lib/abacatepay-pix.test.ts tests/unit/webhook-transparent.test.ts` — all PIX tests pass
2. `npx vitest run` — full suite green (no regressions)
3. `npx tsc --noEmit` — no type errors
4. `npm run lint` — no lint errors
</verification>

<success_criteria>
- createPixCharge, checkPixStatus, simulatePixPayment are exported from abacatepay.ts
- POST /api/purchases/pix creates PIX charge and stores brCode in DB
- GET /api/purchases/pix/status returns PIX status (from DB or AbacatePay)
- Webhook transparent.completed processes PIX payments with idempotency
- Bypass mode works without calling AbacatePay
- All tests pass, tsc clean, lint clean
- docs/status.md updated with PIX backend capabilities
</success_criteria>

<output>
After completion, create `.planning/phases/04-pix-backend/04-01-SUMMARY.md`
</output>
